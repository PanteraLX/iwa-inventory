{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start container",
            "type": "shell",
            "command": "docker exec -it affectionate_agnesi /bin/bash",
            "runOptions": { "runOn": "folderOpen" }
        }
    ]
}